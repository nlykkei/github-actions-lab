name: Reusable workflow 3

on:
  workflow_call:

jobs:
  job-3:  
    name: Job 3
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4